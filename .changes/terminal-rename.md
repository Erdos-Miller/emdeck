- Name a terminal from the terminal itself: click its title in the pane header,
  or double-click its session tab in the Workspaces view. Naming was previously
  reachable only from the Agents overview. A fixed name keeps overriding the
  title the agent reports, and clearing it follows that title again.
