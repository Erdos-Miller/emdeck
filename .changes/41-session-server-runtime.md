- Prepare the session server to own every terminal. Its runtime now handles
  attachments, agent command history and Codex usage reporting, and the session
  protocol moves to version 2. A session server left running from an earlier
  version must be stopped before the new one starts.
